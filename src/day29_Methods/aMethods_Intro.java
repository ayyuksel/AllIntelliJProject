package day29_Methods;

public class aMethods_Intro {
  /*Methods : functions, grouping series of code statement.
    AccessModifier specifier returnType MethodName(Parameter){
                    statement;
                    }

   AccessModifier: public, protected, default,private
   public : open the world

   specifier: static, final, abstract,synchronized
                        can be called through class name
                        ClassName>MethodName
                        Arrays.toString()

    returnType: void, does not return any dataTye
    Parameter : any data type
    without parameter ; if all info is there
    with parameter: additional info needed to for method complete the function

if method passes parameter, we must give argument to the method when we call it



   */





}
